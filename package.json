{
  "name": "scan-diff",
  "version": "0.1.0",
  "description": "Domain-agnostic two-session 3D scan comparison engine: scan, rescan, see what changed.",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "@types/three": "^0.185.0",
    "jsqr": "^1.4.0",
    "three": "^0.185.1"
  }
}
